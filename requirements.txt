# vivijure-local-16gb runtime stack (the CogVideoX consumer door). CogVideoX-5B-I2V image-to-video
# on a single consumer card, plus R2 object I/O. torch/torchvision are installed from the CUDA index in
# deploy/Dockerfile (matched to the card's CUDA line). diffusers/transformers are PINNED to the exact
# set already validated against torch 2.4.1 in the LTX door's container proof: CogVideoXImageToVideoPipeline
# landed in diffusers ~0.30.3 and is present at 0.32.2 (verified), so this door KEEPS the shared cu124
# recipe -- no bump needed (newer diffusers 0.38+ require a newer torch and break on 2.4). transformers
# 4.46.3 is the matching line for this diffusers release.
#
# Phase A = CogVideoX-5B-I2V (THUDM/CogVideoX-5b-I2V), which this recipe supports. Phase B (a FUTURE
# milestone) may add CogVideoX1.5-5B-I2V; if that variant needs a newer diffusers, that is a separate,
# justified pin bump gated on staying on torch 2.4.1+cu124 -- not this milestone.

# --- CogVideoX-5B-I2V engine (HuggingFace diffusers) ---
diffusers==0.32.2          # CogVideoXImageToVideoPipeline present here; validated on torch 2.4.1; 0.38+ breaks on 2.4
transformers==4.46.3       # the transformers line validated with diffusers 0.32.2 on torch 2.4
accelerate>=0.34.0         # enable_model_cpu_offload / enable_sequential_cpu_offload (the consumer-VRAM savers)
safetensors>=0.4.5
sentencepiece>=0.2.0       # CogVideoX T5-XXL text encoder tokenizer
imageio>=2.34.0
imageio-ffmpeg>=0.6.0      # export_to_video
av>=12.0.0                 # frame-accurate clip write

# --- object storage (shared vivijure R2 bucket) ---
boto3>=1.34.0
