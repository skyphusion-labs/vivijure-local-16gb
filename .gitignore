__pycache__/
*.pyc
.venv/
.pytest_cache/
/models/
*.mp4
*.png
.DS_Store

# proof artifacts are evidence -- track them despite the blanket media ignores above
!docs/proof/*.mp4
!docs/proof/*.png

# never commit a real .env (downstream users cp .env.example .env with their creds)
.env
.dev.vars

# coverage artifacts (code-coverage.yml)
.coverage
coverage.xml
